/// Native notification taps are routed by flutter_local_notifications and the
/// Firebase launch-details path; only the web build needs the service-worker
/// bridge (see the web variant).
void initWebNotificationTaps() {}

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- QR pairing scans a code with the camera; attaching an image to a prompt
         can also shoot one (a screen, a whiteboard). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Push notifications (Android 13+ runtime permission). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- No storage permission for picking a screenshot, deliberately.
         image_picker uses the system Android Photo Picker on 13+ and
         ACTION_GET_CONTENT below it; both hand back a single URI the user chose,
         which grants access on its own. Declaring READ_MEDIA_IMAGES would ask
         for the whole library to gain nothing, and drags the app into Play's
         photo-and-video-permissions declaration. -->
    <!-- Picking from the library needs no permission, but shooting a new photo
         does: because CAMERA is declared above (for QR pairing), Android
         enforces it for image_picker's camera source too, and the plugin
         requests it at the point of use. -->

    <application
        android:label="gothalo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Handles Approve/Reject tapped on a notification. The
             flutter_local_notifications plugin ships the class but NOT this
             declaration, so without it Android resolves the action broadcast to
             no component: the button appears, animates on tap, and silently does
             nothing. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- The home-screen widget: blocked/working/done across every paired
             bridge, tapping through to Priority. It only ever renders what the
             app last wrote (updatePeriodMillis is 0 in fleet_widget_info.xml),
             so it schedules nothing and wakes nothing. -->
        <receiver
            android:name=".FleetWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fleet_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

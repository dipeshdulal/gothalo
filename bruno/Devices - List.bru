meta {
  name: Devices - List
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/admin/devices
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

docs {
  Admin-only: list paired devices (id, name, paired_at, last_seen). Same data as
  `gothalo devices list`.
}
